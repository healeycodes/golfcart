// Assert
assert(true)

// Type casts
a = "1"
num(a)
num(("2"))
num(1)
b = 1
str(1)
str("hi!")
c = true
str(c)

// Type
type(1)

// Logging
log(a)
log(a, b)
log(false)

// Stringing native function names
logger = log
assert(str(logger) == "log function")